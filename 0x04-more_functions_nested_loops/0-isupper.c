#include "main.h"

/**
 * _islower - check for lower case letters
 * @c: the character
 * Return:0 or 1
 */

int _islower(int c)
{

if (c >= 65 && c <= 90)
{
        return (1);
}
else
{
        return (0);
}
}
