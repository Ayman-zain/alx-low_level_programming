#include "main.h"

/**
 * mul - this is a function that multiplies two integers
 * @a: first number
 * @b: second number
 * Return: varible r
 */

int mul(int a, int b)
{
int r;

r = a * b;

return (r);
}
